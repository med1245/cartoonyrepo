version = 4

cloudstream {
    description = "أنمي بالعربي - أفلام ومسلسلات الأنمي من animezid.cam"
    authors = listOf("Mehdi Marsaman")

    /**
     * Status int as one of the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta-only
     **/
    status = 1

    tvTypes = listOf("Anime", "AnimeMovie", "TvSeries", "Movie")

    language = "ar"

    iconUrl = "https://animezid.cam/favicon.ico"
}
